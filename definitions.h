namespace definitions
{
    const int SCREEN_WIDTH = 800;
    const int SCREEN_HEIGHT = 450;

    enum turn
    {
        player,
        enemy
    };

    enum actions
    {
        attack,
        magic,
        block
    };
}
